const Button = ({text}) => {
    return (
        <button className='bg-primary text-white p-[12px] border-[1px] border-black rounded-xl'>
            {text}
        </button>
    )
}

export default Button;